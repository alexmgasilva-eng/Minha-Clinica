import React,{useState}from"react";import{Link,useSearchParams}from"react-router-dom";import{base44}from"@/api/base44Client";import{Button}from"@/components/ui/button";import{Input}from"@/components/ui/input";import{Label}from"@/components/ui/label";import{Lock,Loader2,AlertTriangle}from"lucide-react";import AuthLayout from"@/components/AuthLayout";export default function ResetPassword(){const[sp]=useSearchParams(),token=sp.get('token'),[pw,setPw]=useState(''),[confirm,setConfirm]=useState(''),[error,setError]=useState(''),[loading,setLoading]=useState(false);if(!token)return <AuthLayout icon={AlertTriangle} title="Invalid reset link" footer={<Link to="/forgot-password" className="text-primary">Request a new link</Link>}><p className="text-sm text-center">The link is missing or invalid.</p></AuthLayout>;const submit=async e=>{e.preventDefault();if(pw!==confirm){setError('Passwords do not match');return}setLoading(true);try{await base44.auth.resetPassword({resetToken:token,newPassword:pw});window.location.href='/login'}catch(err){setError(err.message||'Failed to reset password')}finally{setLoading(false)}};return <AuthLayout icon={Lock} title="New password" subtitle="Enter your new password below">{error&&<div className="p-3 text-destructive">{error}</div>}<form onSubmit={submit} className="space-y-4"><Label>New Password</Label><Input type="password" value={pw} onChange={e=>setPw(e.target.value)} required/><Label>Confirm Password</Label><Input type="password" value={confirm} onChange={e=>setConfirm(e.target.value)} required/><Button type="submit" className="w-full" disabled={loading}>{loading?<Loader2 className="animate-spin"/>:'Reset password'}</Button></form></AuthLayout>}